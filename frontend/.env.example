# API URL for the backend
# In development, leave empty to use Next.js rewrites (proxying to localhost:8000)
# In production, set to your backend URL (e.g., https://api.example.com)
NEXT_PUBLIC_API_URL=

# Backend URL for rewrites (only used if NEXT_PUBLIC_API_URL is not set)
# Defaults to http://127.0.0.1:8000
BACKEND_URL=http://127.0.0.1:8000
