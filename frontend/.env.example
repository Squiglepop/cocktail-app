# API URL for the backend
# In development, leave empty to default to http://localhost:8000/api (see lib/api.ts)
# In production, set to your backend URL (e.g., https://api.example.com/api)
NEXT_PUBLIC_API_URL=

# Backend URL for Next.js rewrites (server-side proxying)
# Only used if NEXT_PUBLIC_API_URL is not set AND rewrites are configured
BACKEND_URL=http://localhost:8000
